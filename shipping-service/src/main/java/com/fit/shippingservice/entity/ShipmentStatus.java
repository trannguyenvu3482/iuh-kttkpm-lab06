package com.fit.shippingservice.entity;

public enum ShipmentStatus {
    PENDING,
    PROCESSING,
    SHIPPED,
    IN_TRANSIT,
    OUT_FOR_DELIVERY,
    DELIVERED,
    RETURNED,
    CANCELLED,
    FAILED
}